#include "MathCore/mathcore.h"

#include <qmath.h>

//QPoint MathCore::pointOnCircle(const Tank * tank, qreal circleRadius, qreal angle)
//{
//    qreal radAngle = qDegreesToRadians(angle);

//    // parametric equation
//    return QPoint( circleRadius * qCos(radAngle) + tank->x() + tank->rect().width() / 2  ,
//                  circleRadius * qSin(radAngle) + tank->y() + tank->rect().height() / 2 );
//    return QPoint( circleRadius * qCos(radAngle) + tank->x() + Tank::Width / 2  ,
//                   circleRadius * qSin(radAngle) + tank->y() + Tank::Height / 2 );
//}
